Requirement
pip install django djangorestframework djangorestframework-simplejwt



This is sample app to
1. create custom user
2. Create JWT stateless token
3. Refresh JWT token i.e generate new access token
4. Blacklist JWT token i.e can't generate new access token